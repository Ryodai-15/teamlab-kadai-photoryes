<%= form_with model: post_image, local:true do |f| %>
  <%= f.attachment_field :image, class: "w-100" %>
  <div class="input-group mb-3 mt-4">
    <div class="input-group-prepend">
    <!--prepend付与するclass名-->
      <span class="input-group-text" aria-hidden="true"><i class="fas fa-heading"></i></span>
    </div>
    <%= f.text_field :photo_name, class:"form-control", placeholder:"投稿タイトル" %>
  </div>
  <%= f.text_area :caption, class:"form-control", rows:"5", placeholder:"コメントここに" %>
  <hr>
  <%= f.submit '投稿', class:"btn btn-lg btn-block btn-info mt-4" %>
<% end %>